# Forms Redirects

Scripts pequenos usados para alterar o fluxo de navegacao com base no preenchimento do formulario.

## Arquivos

- `conditional-form-redirect.js`: observa um campo select pelo ID `#input_1746017833` e redireciona para a URL da FloraNectar quando o valor selecionado for `Consumidor`.
